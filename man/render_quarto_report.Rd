% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_quarto_report.R
\name{render_quarto_report}
\alias{render_quarto_report}
\title{Render Quarto Report with Enhanced Error Handling}
\usage{
render_quarto_report(template_path, output_file, params)
}
\arguments{
\item{template_path}{Path to the \code{.qmd} template file.}

\item{output_file}{Output file path.}

\item{params}{Named list of report parameters. \code{data_list} (a named list
of data frames) is serialised to a temp \code{.rds} file; all other
parameters are passed as-is via \code{execute_params}.}
}
\value{
Called for its side-effect of writing \code{output_file}.
}
\description{
Renders a Quarto (\code{.qmd}) report using the \code{quarto} R package when
available, falling back to a system call or alternative rendering method
on failure.
}
\details{
Before calling \code{\link[quarto:quarto_render]{quarto::quarto_render()}}, any \code{data_list} element in
\code{params} is written to a temporary \code{.rds} file and replaced with a
\code{data_list_path} string. This is required because
\code{quarto::quarto_render()} validates all \code{execute_params} for \code{NA} values
(via an internal \code{check_params_for_na()} call), and data frames
containing \code{NA}s cannot be passed directly.
}
\keyword{internal}
