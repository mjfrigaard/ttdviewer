# Create Fallback Template

Create Fallback Template

## Usage

``` r
create_fallback_template(format = c("rmarkdown", "quarto"))
```

## Arguments

- format:

  Report format

## Value

Path to created template
