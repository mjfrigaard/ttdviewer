# Report Input Server Module

Report Input Server Module

## Usage

``` r
mod_report_input_server(id)
```

## Arguments

- id:

  Module ID

## Value

A reactive returning the selected format string (`"rmarkdown"` or
`"quarto"`).
