# Inspect Plot Module Server

Renders one plot per (dataset × plot-type)

## Usage

``` r
mod_plot_server(id, ttd)
```

## Arguments

- id:

  Module id

- ttd:

  Reactive or a static named list of data.frames
